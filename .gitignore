# Gradle / build outputs
.gradle/
build/
app/build/

# Android
local.properties
*.apk
*.aab
*.ap_
*.dex

# Signing (never commit)
keystore.properties
*.jks
*.keystore

# IDE
.idea/
*.iml
.vscode/

# OS noise
.DS_Store
Thumbs.db

# Local deploy overrides (may contain real tokens)
deploy/*.local.yaml
deploy/local.env
# Note: machine-local helpers (e.g. a private push script) belong in
# .git/info/exclude, which is not shared.

# Kotlin session dir
.kotlin/
